using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class ApperanceTests
{
 /*   private SphereClass sphereClass1;
    private AppearanceManager ap;

    private ScriptableCube cube;
    private MeshRenderer theRenderer;
    [SerializeField] private Rotation rotation;
    private float RotateSpeed;


    [SetUp]
    public void SetUp()
    {
        ap = new GameObject().AddComponent<AppearanceManager>();
        rotation = new GameObject().AddComponent<Rotation>();

        cube = ScriptableObject.CreateInstance<ScriptableCube>();
        sphereClass1 = new SphereClass
        {
            color = Color.white,

        };
        cube.sphereData = new SphereClass[]
        {
          sphereClass1
        };

        theRenderer = ap.gameObject.AddComponent<MeshRenderer>();
        theRenderer.sharedMaterial = new Material(Shader.Find("Standard"));
      

    }

    [UnityTest]
    public IEnumerator TestColor()
    {
       

            ap.ColorChange(0, cube, theRenderer);

            Assert.AreEqual(theRenderer.sharedMaterial.color, cube.sphereData[0].color);

        
        yield return null;
    }*/
}
